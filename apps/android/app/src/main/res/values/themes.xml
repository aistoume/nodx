<resources>
    <style name="Theme.Nodx" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#2C5282</item>
        <item name="colorSecondary">#F59E0B</item>
    </style>

    <!-- 透明宿主:ProcessTextActivity(文字轮盘/剪贴板面板)用。Material3 是
         AppCompat 后代,AlertDialog 可用;窗口全透明浮在来源 app 上。 -->
    <style name="Theme.Nodx.Translucent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
